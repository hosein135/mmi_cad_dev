// Autogenerated by char on 07/19/01.

module MMI_LLCB (clk,clrb,d,q);
	input	clk;
	input	clrb;
	input	d;
	output	q;

	reg	q;

	always @(clk or d or clrb)
		if (clrb == 0)
			q <= #1 1'b0;
		else if (clk == 0)
			q <= #1 d;

endmodule


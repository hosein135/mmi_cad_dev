// Autogenerated by char on 05/01/00.

module MMI_SFFPB (clk,d,q,s_en,s_in,setb);
	input	clk;
	input	d;
	output	q;
	input	s_en;
	input	s_in;
	input	setb;

	reg	q;

	always @(posedge clk or negedge setb)
		if (setb == 0)
			q <= #1 1'b1;
		else
			q <= #1 d;

endmodule


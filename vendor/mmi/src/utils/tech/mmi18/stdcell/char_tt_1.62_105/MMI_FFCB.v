// Autogenerated by char on 05/12/00.

module MMI_FFCB (clk,clrb,d,q);
	input	clk;
	input	clrb;
	input	d;
	output	q;

	reg	q;

	always @(posedge clk or negedge clrb)
		if (clrb == 0)
			q <= #1 1'b0;
		else
			q <= #1 d;

endmodule


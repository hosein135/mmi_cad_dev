// Autogenerated by char on 05/11/00.

module MMI_SFFSB (clk,d,q,s_en,s_in,s_out,setb);
	input	clk;
	input	d;
	output	q;
	input	s_en;
	input	s_in;
	output	s_out;
	input	setb;

	reg	q;

	always @(posedge clk or negedge setb)
		if (setb == 0)
			q <= #1 1'b1;
		else
			q <= #1 d;

endmodule


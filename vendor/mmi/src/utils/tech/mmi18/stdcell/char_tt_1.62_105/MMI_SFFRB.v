// Autogenerated by char on 05/01/00.

module MMI_SFFRB (clk,clrb,d,q,s_en,s_in);
	input	clk;
	input	clrb;
	input	d;
	output	q;
	input	s_en;
	input	s_in;

	reg	q;

	always @(posedge clk or negedge clrb)
		if (clrb == 0)
			q <= #1 1'b0;
		else
			q <= #1 d;

endmodule


// ************************************************************************
// 
// Copyright (c) 1995-2002 Juniper Networks, Inc. All rights reserved.
// 
// Permission is hereby granted, without written agreement and without
// license or royalty fees, to use, copy, modify, and distribute this
// software and its documentation for any purpose, provided that the
// above copyright notice and the following three paragraphs appear in
// all copies of this software.
// 
// IN NO EVENT SHALL JUNIPER NETWORKS, INC. BE LIABLE TO ANY PARTY FOR
// DIRECT, INDIRECT, SPECIAL, INCIDENTAL, OR CONSEQUENTIAL DAMAGES
// ARISING OUT OF THE USE OF THIS SOFTWARE AND ITS DOCUMENTATION, EVEN IF
// JUNIPER NETWORKS, INC. HAS BEEN ADVISED OF THE POSSIBILITY OF SUCH
// DAMAGE.
// 
// JUNIPER NETWORKS, INC. SPECIFICALLY DISCLAIMS ANY WARRANTIES,
// INCLUDING, BUT NOT LIMITED TO, THE IMPLIED WARRANTIES OF
// MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE, AND
// NON-INFRINGEMENT.
// 
// THE SOFTWARE PROVIDED HEREUNDER IS ON AN "AS IS" BASIS, AND JUNIPER
// NETWORKS, INC. HAS NO OBLIGATION TO PROVIDE MAINTENANCE, SUPPORT,
// UPDATES, ENHANCEMENTS, OR MODIFICATIONS.
// 
// ************************************************************************

/* DO NOT EDIT THIS FILE */
/* This file is generated from v2nl_rewrites.ee. */


#include "stdpccts.h"


static
nl_walk_status
__ref_to_lref_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (replace
            (REF =ref)
            [LREF $ref.object])
  */
  {
    nl_ast __ref_node_start = NULL;
    nl_ast __ref_node_end = NULL;

    __tree = *__root_p;
    /* (replace */
    /* beginning pattern (REF...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "(REF" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_ref);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      __ref_node_start = __prev_start;
      __ref_node_end = __prev_end;
      /* matching ")" */
      __match = (__tree == NULL);
      if ( ! __match ) {
	break;
      }
      /* end pattern (REF...) */
    } while (0);
    if ( __match ) {
      /* (replace...) rule matched, now construct replacement */
      {
	char *__file = nl_ast_file (__tree);
	int __line = nl_ast_line (__tree);
	nl_ast __new_tree_start = NULL;
	nl_ast __new_tree_end = NULL;
	nl_ast __sibling = NULL;
	__new_tree_start = nl_ast_sibling (__tree);
	__sibling = __new_tree_start;
	/* saving __sibling */
	{
	  nl_ast __sibling = NULL;
	  __new_tree_start = (nl_ast) nl_ref_ast_create (nl_token_lref, nl_ref_ast_object ((nl_ref_ast) __ref_node_start));
	  __new_tree_end = __new_tree_start;
	  nl_ast_set_file_line (__new_tree_start, __file, __line);
	}
	if ( __new_tree_end != NULL ) {
	  nl_ast_set_sibling (__new_tree_end, __sibling); 
	}
	*__root_p = __new_tree_start;
	__tree = __new_tree_end;
      }
      /* end of (replace...) */
    }
  }
  return nl_walk_status_continue;
}

static
nl_ast
ref_to_lref (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ref_to_lref_postwalker, NULL);
  return root;
}




static
nl_walk_status
__v2nl_lhs_ref_to_lref_prewalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: ((BLOCK_ASSIGN NONBLOCK_ASSIGN)
            *
            (call-rewrite ref_to_lref)
            &rest)
  */
  {

    __tree = *__root_p;
    /* beginning pattern ((BLOCK_ASSIGN NONBLOCK_ASSIGN)...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "((BLOCK_ASSIGN NONBLOCK_ASSIGN)" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_block_assign ||
		    __token == nl_token_nonblock_assign);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      __match = (__tree != NULL);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      __tree = ref_to_lref (__tree);
      nl_ast_set_sibling (__prev_end, __tree);
      __tree = __tree;
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* &rest */
      __prev_start = __prev_end = __tree;
      /* have to scan to the end of the list to set __prev_end */
       if ( __prev_start != NULL ) {
	nl_ast __temp = __prev_start;
	while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
	  __prev_end = __temp;
	}
      }
      /* matching ")" */
      /* &rest is in effect, so always match */
      __match = 1;
      /* end pattern ((BLOCK_ASSIGN NONBLOCK_ASSIGN)...) */
    } while (0);
  }
    if ( __match ) {
    __status = nl_walk_status_skip;
    }
  return nl_walk_status_continue;
}

nl_ast
v2nl_lhs_ref_to_lref (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, __v2nl_lhs_ref_to_lref_prewalker, NULL, NULL);
  return root;
}




static
nl_walk_status
__v2nl_rewrite_var_shift_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            (let "int amount;"
              (replace
               (SHL $lhs $rhs)
               (if "v2nl_eval_expr ($rhs, &amount)"
                   (SHL $lhs
                        [NUMBER "amount"])
                 (VARSHL $lhs $rhs))))
            (let "int amount;"
              (replace
               (SHR $lhs $rhs)
               (if "v2nl_eval_expr ($rhs, &amount)"
                   (SHR $lhs
                        [NUMBER "amount"])
                 (VARSHR $lhs $rhs)))))
  */
  {
    nl_ast __rhs_node_start = NULL;
    nl_ast __rhs_node_end = NULL;
    nl_ast __lhs_node_start = NULL;
    nl_ast __lhs_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* (let */
      {
	int amount;

	/* (replace */
	/* beginning pattern (SHL...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(SHL" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_shl);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* matching "$lhs" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __lhs_node_start = __tree;
	    __lhs_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching "$rhs" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __rhs_node_start = __tree;
	    __rhs_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (SHL...) */
	} while (0);
	if ( __match ) {
	  /* (replace...) rule matched, now construct replacement */
	  {
	    char *__file = nl_ast_file (__tree);
	    int __line = nl_ast_line (__tree);
	    nl_ast __new_tree_start = NULL;
	    nl_ast __new_tree_end = NULL;
	    nl_ast __sibling = NULL;
	    __new_tree_start = nl_ast_sibling (__tree);
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      if ( v2nl_eval_expr (__rhs_node_start, &amount) ) {
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_number_ast_create (amount);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __lhs_node_start;
		  __new_tree_end = __lhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token SHL */
		{
		  nl_token __token = nl_token_shl;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	      else {
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __rhs_node_start;
		  __new_tree_end = __rhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __lhs_node_start;
		  __new_tree_end = __lhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token VARSHL */
		{
		  nl_token __token = nl_token_varshl;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    *__root_p = __new_tree_start;
	    __tree = __new_tree_end;
	  }
	  /* end of (replace...) */
	}
	/* end of (let...) */
      }
      if ( __match ) {
	break;
      }
      /* (let */
      {
	int amount;

	/* (replace */
	/* beginning pattern (SHR...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(SHR" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_shr);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* matching "$lhs" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __lhs_node_start = __tree;
	    __lhs_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching "$rhs" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __rhs_node_start = __tree;
	    __rhs_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (SHR...) */
	} while (0);
	if ( __match ) {
	  /* (replace...) rule matched, now construct replacement */
	  {
	    char *__file = nl_ast_file (__tree);
	    int __line = nl_ast_line (__tree);
	    nl_ast __new_tree_start = NULL;
	    nl_ast __new_tree_end = NULL;
	    nl_ast __sibling = NULL;
	    __new_tree_start = nl_ast_sibling (__tree);
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      if ( v2nl_eval_expr (__rhs_node_start, &amount) ) {
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_number_ast_create (amount);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __lhs_node_start;
		  __new_tree_end = __lhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token SHR */
		{
		  nl_token __token = nl_token_shr;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	      else {
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __rhs_node_start;
		  __new_tree_end = __rhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = __lhs_node_start;
		  __new_tree_end = __lhs_node_end;
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token VARSHR */
		{
		  nl_token __token = nl_token_varshr;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    *__root_p = __new_tree_start;
	    __tree = __new_tree_end;
	  }
	  /* end of (replace...) */
	}
	/* end of (let...) */
      }
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
v2nl_rewrite_var_shift (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __v2nl_rewrite_var_shift_postwalker, NULL);
  return root;
}




static
nl_walk_status
__v2nl_if_to_case_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (replace
            (IF $test_expr $then_stmt &optional $else_stmt)
            (CASE
             (OR_REDUCE $test_expr)
             (LIST PARALLEL_CASE)
             (CASE_ITEM
              (LIST
               (BIN 1
                    [VNUM "1"]))
              $then_stmt)
             (if "$else_stmt != NULL"
                 (DEFAULT $else_stmt))))
  */
  {
    nl_ast __test_expr_node_start = NULL;
    nl_ast __test_expr_node_end = NULL;
    nl_ast __else_stmt_node_start = NULL;
    nl_ast __else_stmt_node_end = NULL;
    nl_ast __then_stmt_node_start = NULL;
    nl_ast __then_stmt_node_end = NULL;

    __tree = *__root_p;
    /* (replace */
    /* beginning pattern (IF...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "(IF" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_if);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      /* matching "$test_expr" */
      __match = (__tree != NULL);
      if ( __match ) {
	__test_expr_node_start = __tree;
	__test_expr_node_end = __tree;
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* matching "$then_stmt" */
      __match = (__tree != NULL);
      if ( __match ) {
	__then_stmt_node_start = __tree;
	__then_stmt_node_end = __tree;
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* &optional */
      /* matching "$else_stmt" */
      __match = (__tree != NULL);
      if ( __match ) {
	__else_stmt_node_start = __tree;
	__else_stmt_node_end = __tree;
      }
      /* &optional is in effect */
      /* only advance to sibling if matched */
      if ( __match ) {
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
      }
      __match = 1;
      /* matching ")" */
      __match = (__tree == NULL);
      if ( ! __match ) {
	break;
      }
      /* end pattern (IF...) */
    } while (0);
    if ( __match ) {
      /* (replace...) rule matched, now construct replacement */
      {
	char *__file = nl_ast_file (__tree);
	int __line = nl_ast_line (__tree);
	nl_ast __new_tree_start = NULL;
	nl_ast __new_tree_end = NULL;
	nl_ast __sibling = NULL;
	__new_tree_start = nl_ast_sibling (__tree);
	__sibling = __new_tree_start;
	/* saving __sibling */
	{
	  nl_ast __sibling = NULL;
	  __new_tree_start = NULL;
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    if ( __else_stmt_node_start != NULL ) {
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __else_stmt_node_start;
		__new_tree_end = __else_stmt_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token DEFAULT */
	      {
		nl_token __token = nl_token_default;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __then_stmt_node_start;
	      __new_tree_end = __then_stmt_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_vnum_ast_create ("1");
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_number_ast_create (1);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token BIN */
		{
		  nl_token __token = nl_token_bin;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token LIST */
	      {
		nl_token __token = nl_token_list;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token CASE_ITEM */
	    {
	      nl_token __token = nl_token_case_item;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = nl_ast_create (nl_token_parallel_case);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token LIST */
	    {
	      nl_token __token = nl_token_list;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __test_expr_node_start;
	      __new_tree_end = __test_expr_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token OR_REDUCE */
	    {
	      nl_token __token = nl_token_or_reduce;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  /* constructing node with token CASE */
	  {
	    nl_token __token = nl_token_case;
	    nl_ast __child = __new_tree_start;
	    __new_tree_start = nl_ast_create (__token);
	    nl_ast_set_file_line (__new_tree_start, __file, __line);
	    __new_tree_end = __new_tree_start;
	    nl_ast_set_child (__new_tree_start, __child);
	  }
	}
	if ( __new_tree_end != NULL ) {
	  nl_ast_set_sibling (__new_tree_end, __sibling); 
	}
	*__root_p = __new_tree_start;
	__tree = __new_tree_end;
      }
      /* end of (replace...) */
    }
  }
  return nl_walk_status_continue;
}

nl_ast
v2nl_if_to_case (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __v2nl_if_to_case_postwalker, NULL);
  return root;
}





// ************************************************************************
// 
// Copyright (c) 1995-2002 Juniper Networks, Inc. All rights reserved.
// 
// Permission is hereby granted, without written agreement and without
// license or royalty fees, to use, copy, modify, and distribute this
// software and its documentation for any purpose, provided that the
// above copyright notice and the following three paragraphs appear in
// all copies of this software.
// 
// IN NO EVENT SHALL JUNIPER NETWORKS, INC. BE LIABLE TO ANY PARTY FOR
// DIRECT, INDIRECT, SPECIAL, INCIDENTAL, OR CONSEQUENTIAL DAMAGES
// ARISING OUT OF THE USE OF THIS SOFTWARE AND ITS DOCUMENTATION, EVEN IF
// JUNIPER NETWORKS, INC. HAS BEEN ADVISED OF THE POSSIBILITY OF SUCH
// DAMAGE.
// 
// JUNIPER NETWORKS, INC. SPECIFICALLY DISCLAIMS ANY WARRANTIES,
// INCLUDING, BUT NOT LIMITED TO, THE IMPLIED WARRANTIES OF
// MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE, AND
// NON-INFRINGEMENT.
// 
// THE SOFTWARE PROVIDED HEREUNDER IS ON AN "AS IS" BASIS, AND JUNIPER
// NETWORKS, INC. HAS NO OBLIGATION TO PROVIDE MAINTENANCE, SUPPORT,
// UPDATES, ENHANCEMENTS, OR MODIFICATIONS.
// 
// ************************************************************************

/* DO NOT EDIT THIS FILE */
/* This file is generated from ast2g_rewrites.ee. */


#include "port.h"
#include "error.h"
#include "mem.h"
#include "ar.h"
#include "str.h"
#include "hashtab.h"
#include "nl.h"
#include "str.h"
#include "ssa.h"
#include "ast2g.h"
#include "ast2g_int.h"


static
nl_walk_status
__ast2g_eliminate_logical_ops_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            (replace
             (ANDAND $arg1 $arg2)
             (AND
              (OR_REDUCE $arg1)
              (OR_REDUCE $arg2)))
            (replace
             (OROR $arg1 $arg2)
             (OR
              (OR_REDUCE $arg1)
              (OR_REDUCE $arg2)))
            (replace
             (LOGNOT $arg)
             (BITNOT
              (OR_REDUCE $arg))))
  */
  {
    nl_ast __arg1_node_start = NULL;
    nl_ast __arg1_node_end = NULL;
    nl_ast __arg2_node_start = NULL;
    nl_ast __arg2_node_end = NULL;
    nl_ast __arg_node_start = NULL;
    nl_ast __arg_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* (replace */
      /* beginning pattern (ANDAND...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(ANDAND" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_andand);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$arg1" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg1_node_start = __tree;
	  __arg1_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching "$arg2" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg2_node_start = __tree;
	  __arg2_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (ANDAND...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __arg2_node_start;
		__new_tree_end = __arg2_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token OR_REDUCE */
	      {
		nl_token __token = nl_token_or_reduce;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __arg1_node_start;
		__new_tree_end = __arg1_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token OR_REDUCE */
	      {
		nl_token __token = nl_token_or_reduce;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token AND */
	    {
	      nl_token __token = nl_token_and;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* beginning pattern (OROR...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(OROR" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_oror);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$arg1" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg1_node_start = __tree;
	  __arg1_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching "$arg2" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg2_node_start = __tree;
	  __arg2_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (OROR...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __arg2_node_start;
		__new_tree_end = __arg2_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token OR_REDUCE */
	      {
		nl_token __token = nl_token_or_reduce;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __arg1_node_start;
		__new_tree_end = __arg1_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token OR_REDUCE */
	      {
		nl_token __token = nl_token_or_reduce;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token OR */
	    {
	      nl_token __token = nl_token_or;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* beginning pattern (LOGNOT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(LOGNOT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_lognot);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$arg" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg_node_start = __tree;
	  __arg_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (LOGNOT...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __arg_node_start;
		__new_tree_end = __arg_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token OR_REDUCE */
	      {
		nl_token __token = nl_token_or_reduce;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token BITNOT */
	    {
	      nl_token __token = nl_token_bitnot;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_eliminate_logical_ops (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_eliminate_logical_ops_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_simplify_reduce_ops_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            (replace
             (and
              ((OR_REDUCE AND_REDUCE XOR_REDUCE)
               $arg)
              (test "$arg.width == 1"))
             $arg)
            (replace
             (and
              (XNOR_REDUCE $arg)
              (test "$arg.width == 1"))
             (BITNOT $arg))
            (replace
             (and
              (OR_REDUCE
               (CONCAT $arg1 $arg2))
              (test "$arg1.width == 1 && $arg2.width == 1"))
             (OR $arg1 $arg2))
            (replace
             (and
              (AND_REDUCE
               (CONCAT $arg1 $arg2))
              (test "$arg1.width == 1 && $arg2.width == 1"))
             (AND $arg1 $arg2))
            (replace
             (and
              (XOR_REDUCE
               (CONCAT $arg1 $arg2))
              (test "$arg1.width == 1 && $arg2.width == 1"))
             (XOR $arg1 $arg2))
            (replace
             (and
              (XNOR_REDUCE
               (CONCAT $arg1 $arg2))
              (test "$arg1.width == 1 && $arg2.width == 1"))
             (XNOR $arg1 $arg2)))
  */
  {
    nl_ast __arg1_node_start = NULL;
    nl_ast __arg1_node_end = NULL;
    nl_ast __arg2_node_start = NULL;
    nl_ast __arg2_node_end = NULL;
    nl_ast __arg_node_start = NULL;
    nl_ast __arg_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* (replace */
      /* (and */
      do {
	/* beginning pattern ((OR_REDUCE AND_REDUCE XOR_REDUCE)...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "((OR_REDUCE AND_REDUCE XOR_REDUCE)" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_or_reduce ||
			__token == nl_token_and_reduce ||
			__token == nl_token_xor_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* matching "$arg" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __arg_node_start = __tree;
	    __arg_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern ((OR_REDUCE AND_REDUCE XOR_REDUCE)...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg.width == 1) */
	__match = (nl_ast_width (__arg_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __arg_node_start;
	    __new_tree_end = __arg_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* (and */
      do {
	/* beginning pattern (XNOR_REDUCE...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(XNOR_REDUCE" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_xnor_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* matching "$arg" */
	  __match = (__tree != NULL);
	  if ( __match ) {
	    __arg_node_start = __tree;
	    __arg_node_end = __tree;
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (XNOR_REDUCE...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg.width == 1) */
	__match = (nl_ast_width (__arg_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg_node_start;
	      __new_tree_end = __arg_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token BITNOT */
	    {
	      nl_token __token = nl_token_bitnot;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* (and */
      do {
	/* beginning pattern (OR_REDUCE...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(OR_REDUCE" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_or_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* beginning pattern (CONCAT...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(CONCAT" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_concat);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* matching "$arg1" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg1_node_start = __tree;
	      __arg1_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching "$arg2" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg2_node_start = __tree;
	      __arg2_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (CONCAT...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (OR_REDUCE...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg1.width == 1 && $arg2.width == 1) */
	__match = (nl_ast_width (__arg1_node_start) == 1 && nl_ast_width (__arg2_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg2_node_start;
	      __new_tree_end = __arg2_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg1_node_start;
	      __new_tree_end = __arg1_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token OR */
	    {
	      nl_token __token = nl_token_or;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* (and */
      do {
	/* beginning pattern (AND_REDUCE...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(AND_REDUCE" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_and_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* beginning pattern (CONCAT...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(CONCAT" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_concat);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* matching "$arg1" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg1_node_start = __tree;
	      __arg1_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching "$arg2" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg2_node_start = __tree;
	      __arg2_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (CONCAT...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (AND_REDUCE...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg1.width == 1 && $arg2.width == 1) */
	__match = (nl_ast_width (__arg1_node_start) == 1 && nl_ast_width (__arg2_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg2_node_start;
	      __new_tree_end = __arg2_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg1_node_start;
	      __new_tree_end = __arg1_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token AND */
	    {
	      nl_token __token = nl_token_and;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* (and */
      do {
	/* beginning pattern (XOR_REDUCE...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(XOR_REDUCE" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_xor_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* beginning pattern (CONCAT...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(CONCAT" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_concat);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* matching "$arg1" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg1_node_start = __tree;
	      __arg1_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching "$arg2" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg2_node_start = __tree;
	      __arg2_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (CONCAT...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (XOR_REDUCE...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg1.width == 1 && $arg2.width == 1) */
	__match = (nl_ast_width (__arg1_node_start) == 1 && nl_ast_width (__arg2_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg2_node_start;
	      __new_tree_end = __arg2_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg1_node_start;
	      __new_tree_end = __arg1_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token XOR */
	    {
	      nl_token __token = nl_token_xor;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* (and */
      do {
	/* beginning pattern (XNOR_REDUCE...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(XNOR_REDUCE" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_xnor_reduce);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* beginning pattern (CONCAT...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(CONCAT" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_concat);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* matching "$arg1" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg1_node_start = __tree;
	      __arg1_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching "$arg2" */
	    __match = (__tree != NULL);
	    if ( __match ) {
	      __arg2_node_start = __tree;
	      __arg2_node_end = __tree;
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (CONCAT...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (XNOR_REDUCE...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	/* (test $arg1.width == 1 && $arg2.width == 1) */
	__match = (nl_ast_width (__arg1_node_start) == 1 && nl_ast_width (__arg2_node_start) == 1);
	if ( ! __match ) {
	  break;
	}
	/* end of (and...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg2_node_start;
	      __new_tree_end = __arg2_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __arg1_node_start;
	      __new_tree_end = __arg1_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token XNOR */
	    {
	      nl_token __token = nl_token_xnor;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_simplify_reduce_ops (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_simplify_reduce_ops_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_explicit_case_prewalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (CASE
            (replace
             (or
              (IN =in)
              (REF =ref))
             (BIN 1
                  [VNUM "1"]))
            *
            (repeat
             (CASE_ITEM
              (replace
               (LIST
                (repeat
                 (replace $expr
                          (EQ2 $expr
                               (if "$ref != NULL"
                                   [REF $ref.object]
                                 [IN $in.in_index]))))
                =tests)
               (LIST
                (if "$tests.sibling == NULL" $tests
                  (OR_REDUCE
                   (CONCAT $tests)))))
              *))
            &optional *)
  */
  {
    nl_ast __tests_node_start = NULL;
    nl_ast __tests_node_end = NULL;
    nl_ast __ref_node_start = NULL;
    nl_ast __ref_node_end = NULL;
    nl_ast __in_node_start = NULL;
    nl_ast __in_node_end = NULL;
    nl_ast __expr_node_start = NULL;
    nl_ast __expr_node_end = NULL;

    __tree = *__root_p;
    /* beginning pattern (CASE...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "(CASE" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_case);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      /* (replace */
      /* (or */
      do {
	/* beginning pattern (IN...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(IN" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_in);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  __in_node_start = __prev_start;
	  __in_node_end = __prev_end;
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (IN...) */
	} while (0);
	if ( __match ) {
	  break;
	}
	/* beginning pattern (REF...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(REF" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_ref);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  __ref_node_start = __prev_start;
	  __ref_node_end = __prev_end;
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (REF...) */
	} while (0);
	if ( __match ) {
	  break;
	}
	/* end of (or...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = (nl_ast) nl_vnum_ast_create ("1");
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = (nl_ast) nl_number_ast_create (1);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token BIN */
	    {
	      nl_token __token = nl_token_bin;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  nl_ast_set_child (__prev_start, __new_tree_start);
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      __match = (__tree != NULL);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* (repeat */
      {
	nl_ast __temp = __prev_start;
	nl_ast __prev_start = __temp;
	int __first = 1;
	do {
	  /* beginning pattern (CASE_ITEM...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(CASE_ITEM" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_case_item);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* (replace */
	    /* beginning pattern (LIST...) */
	    do {
	      nl_ast __temp = __tree;
	      nl_ast __tree = __temp;
	      nl_ast __prev_start = NULL;
	      nl_ast __prev_end = NULL;

	      /* matching "(LIST" */
	      __match = 0;
	      if ( __tree != NULL ) {
		nl_token __token = nl_ast_token (__tree);
		__match =  (__token == nl_token_list);
	      }
	      if ( ! __match ) {
		break;
	      }
	      __prev_start = __prev_end = __tree;
	      __tree = nl_ast_child (__tree);

	      /* (repeat */
	      {
		nl_ast __temp = __prev_start;
		nl_ast __prev_start = __temp;
		int __first = 1;
		do {
		  /* (replace */
		  /* matching "$expr" */
		  __match = (__tree != NULL);
		  if ( __match ) {
		    __expr_node_start = __tree;
		    __expr_node_end = __tree;
		  }
		  if ( __match ) {
		    /* (replace...) rule matched, now construct replacement */
		    {
		      char *__file = nl_ast_file (__tree);
		      int __line = nl_ast_line (__tree);
		      nl_ast __new_tree_start = NULL;
		      nl_ast __new_tree_end = NULL;
		      nl_ast __sibling = NULL;
		      __new_tree_start = nl_ast_sibling (__tree);
		      __sibling = __new_tree_start;
		      /* saving __sibling */
		      {
			nl_ast __sibling = NULL;
			__new_tree_start = NULL;
			__sibling = __new_tree_start;
			/* saving __sibling */
			{
			  nl_ast __sibling = NULL;
			  if ( __ref_node_start != NULL ) {
			    __new_tree_start = (nl_ast) nl_ref_ast_create (nl_token_ref, nl_ref_ast_object ((nl_ref_ast) __ref_node_start));
			    __new_tree_end = __new_tree_start;
			    nl_ast_set_file_line (__new_tree_start, __file, __line);
			  }
			  else {
			    __new_tree_start = (nl_ast) nl_in_ast_create (nl_in_ast_index ((nl_in_ast) __in_node_start));
			    __new_tree_end = __new_tree_start;
			    nl_ast_set_file_line (__new_tree_start, __file, __line);
			  }
			}
			if ( __new_tree_end != NULL ) {
			  nl_ast_set_sibling (__new_tree_end, __sibling); 
			}
			__sibling = __new_tree_start;
			/* saving __sibling */
			{
			  nl_ast __sibling = NULL;
			  __new_tree_start = __expr_node_start;
			  __new_tree_end = __expr_node_end;
			}
			if ( __new_tree_end != NULL ) {
			  nl_ast_set_sibling (__new_tree_end, __sibling); 
			}
			/* constructing node with token EQ2 */
			{
			  nl_token __token = nl_token_eq2;
			  nl_ast __child = __new_tree_start;
			  __new_tree_start = nl_ast_create (__token);
			  nl_ast_set_file_line (__new_tree_start, __file, __line);
			  __new_tree_end = __new_tree_start;
			  nl_ast_set_child (__new_tree_start, __child);
			}
		      }
		      if ( __new_tree_end != NULL ) {
			nl_ast_set_sibling (__new_tree_end, __sibling);	
		      }
		      if ( __first ) {
			nl_ast_set_child (__prev_start, __new_tree_start);
		      }
		      else {
			nl_ast_set_sibling (__prev_start, __new_tree_start);
		      }
		      __tree = __new_tree_end;
		    }
		    /* end of (replace...) */
		  }
		  if ( ! __match ) {
		    break;
		  }
		  __prev_start = __prev_end = __tree;
		  __tree = nl_ast_sibling (__tree);
		  __match = (__tree != NULL);
		  __first = 0;
		/* end of (repeat...) */
		} while (__match);

	      }
	      /* have to set __prev_start to point to the beginning of the repeat */
	      __prev_start = nl_ast_child (__prev_start);
	      /* repeat construct always matches */
	      __match = 1;
	      __tests_node_start = __prev_start;
	      __tests_node_end = __prev_end;
	      /* matching ")" */
	      __match = (__tree == NULL);
	      if ( ! __match ) {
		break;
	      }
	      /* end pattern (LIST...) */
	    } while (0);
	    if ( __match ) {
	      /* (replace...) rule matched, now construct replacement */
	      {
		char *__file = nl_ast_file (__tree);
		int __line = nl_ast_line (__tree);
		nl_ast __new_tree_start = NULL;
		nl_ast __new_tree_end = NULL;
		nl_ast __sibling = NULL;
		__new_tree_start = nl_ast_sibling (__tree);
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = NULL;
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    if ( nl_ast_sibling (__tests_node_start) == NULL ) {
		      __new_tree_start = __tests_node_start;
		      __new_tree_end = __tests_node_end;
		    }
		    else {
		      __new_tree_start = NULL;
		      __sibling = __new_tree_start;
		      /* saving __sibling */
		      {
			nl_ast __sibling = NULL;
			__new_tree_start = NULL;
			__sibling = __new_tree_start;
			/* saving __sibling */
			{
			  nl_ast __sibling = NULL;
			  __new_tree_start = __tests_node_start;
			  __new_tree_end = __tests_node_end;
			}
			if ( __new_tree_end != NULL ) {
			  nl_ast_set_sibling (__new_tree_end, __sibling); 
			}
			/* constructing node with token CONCAT */
			{
			  nl_token __token = nl_token_concat;
			  nl_ast __child = __new_tree_start;
			  __new_tree_start = nl_ast_create (__token);
			  nl_ast_set_file_line (__new_tree_start, __file, __line);
			  __new_tree_end = __new_tree_start;
			  nl_ast_set_child (__new_tree_start, __child);
			}
		      }
		      if ( __new_tree_end != NULL ) {
			nl_ast_set_sibling (__new_tree_end, __sibling);	
		      }
		      /* constructing node with token OR_REDUCE */
		      {
			nl_token __token = nl_token_or_reduce;
			nl_ast __child = __new_tree_start;
			__new_tree_start = nl_ast_create (__token);
			nl_ast_set_file_line (__new_tree_start, __file, __line);
			__new_tree_end = __new_tree_start;
			nl_ast_set_child (__new_tree_start, __child);
		      }
		    }
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  /* constructing node with token LIST */
		  {
		    nl_token __token = nl_token_list;
		    nl_ast __child = __new_tree_start;
		    __new_tree_start = nl_ast_create (__token);
		    nl_ast_set_file_line (__new_tree_start, __file, __line);
		    __new_tree_end = __new_tree_start;
		    nl_ast_set_child (__new_tree_start, __child);
		  }
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		nl_ast_set_child (__prev_start, __new_tree_start);
		__tree = __new_tree_end;
	      }
	      /* end of (replace...) */
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    __match = (__tree != NULL);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (CASE_ITEM...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  __match = (__tree != NULL);
	  __first = 0;
	/* end of (repeat...) */
	} while (__match);

      }
      /* have to set __prev_start to point to the beginning of the repeat */
      __prev_start = nl_ast_sibling (__prev_end);
      /* repeat construct always matches */
      __match = 1;
      /* &optional */
      __match = (__tree != NULL);
      /* &optional is in effect */
      /* only advance to sibling if matched */
      if ( __match ) {
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
      }
      __match = 1;
      /* matching ")" */
      __match = (__tree == NULL);
      if ( ! __match ) {
	break;
      }
      /* end pattern (CASE...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_explicit_case (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, __ast2g_explicit_case_prewalker, NULL, NULL);
  return root;
}




static
nl_walk_status
__ast2g_rewrite_clocks_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (replace
            (ALWAYS
             (LIST $clk_edge)
             $body)
            (ALWAYS
             (LIST)
             (CASE $clk_edge
                   (LIST)
                   (CASE_ITEM
                    (LIST
                     (BIN 1
                          [VNUM "1"]))
                    $body))))
  */
  {
    nl_ast __clk_edge_node_start = NULL;
    nl_ast __clk_edge_node_end = NULL;
    nl_ast __body_node_start = NULL;
    nl_ast __body_node_end = NULL;

    __tree = *__root_p;
    /* (replace */
    /* beginning pattern (ALWAYS...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "(ALWAYS" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_always);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      /* beginning pattern (LIST...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(LIST" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_list);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$clk_edge" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __clk_edge_node_start = __tree;
	  __clk_edge_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (LIST...) */
      } while (0);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* matching "$body" */
      __match = (__tree != NULL);
      if ( __match ) {
	__body_node_start = __tree;
	__body_node_end = __tree;
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* matching ")" */
      __match = (__tree == NULL);
      if ( ! __match ) {
	break;
      }
      /* end pattern (ALWAYS...) */
    } while (0);
    if ( __match ) {
      /* (replace...) rule matched, now construct replacement */
      {
	char *__file = nl_ast_file (__tree);
	int __line = nl_ast_line (__tree);
	nl_ast __new_tree_start = NULL;
	nl_ast __new_tree_end = NULL;
	nl_ast __sibling = NULL;
	__new_tree_start = nl_ast_sibling (__tree);
	__sibling = __new_tree_start;
	/* saving __sibling */
	{
	  nl_ast __sibling = NULL;
	  __new_tree_start = NULL;
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = __body_node_start;
		__new_tree_end = __body_node_end;
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = NULL;
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = (nl_ast) nl_vnum_ast_create ("1");
		    __new_tree_end = __new_tree_start;
		    nl_ast_set_file_line (__new_tree_start, __file, __line);
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = (nl_ast) nl_number_ast_create (1);
		    __new_tree_end = __new_tree_start;
		    nl_ast_set_file_line (__new_tree_start, __file, __line);
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  /* constructing node with token BIN */
		  {
		    nl_token __token = nl_token_bin;
		    nl_ast __child = __new_tree_start;
		    __new_tree_start = nl_ast_create (__token);
		    nl_ast_set_file_line (__new_tree_start, __file, __line);
		    __new_tree_end = __new_tree_start;
		    nl_ast_set_child (__new_tree_start, __child);
		  }
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token LIST */
		{
		  nl_token __token = nl_token_list;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token CASE_ITEM */
	      {
		nl_token __token = nl_token_case_item;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      /* constructing node with token LIST */
	      {
		nl_token __token = nl_token_list;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __clk_edge_node_start;
	      __new_tree_end = __clk_edge_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token CASE */
	    {
	      nl_token __token = nl_token_case;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    /* constructing node with token LIST */
	    {
	      nl_token __token = nl_token_list;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  /* constructing node with token ALWAYS */
	  {
	    nl_token __token = nl_token_always;
	    nl_ast __child = __new_tree_start;
	    __new_tree_start = nl_ast_create (__token);
	    nl_ast_set_file_line (__new_tree_start, __file, __line);
	    __new_tree_end = __new_tree_start;
	    nl_ast_set_child (__new_tree_start, __child);
	  }
	}
	if ( __new_tree_end != NULL ) {
	  nl_ast_set_sibling (__new_tree_end, __sibling); 
	}
	*__root_p = __new_tree_start;
	__tree = __new_tree_end;
      }
      /* end of (replace...) */
    }
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_rewrite_clocks (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_rewrite_clocks_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_flip_cases_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (replace
            (CASE
             (BITNOT $expr)
             (LIST &rest)
             =pragmas
             (CASE_ITEM
              (LIST
               (BIN 1
                    [VNUM "1"]))
              $body)
             &optional
             (DEFAULT &rest)
             =default)
            (CASE $expr $pragmas
                  (CASE_ITEM
                   (LIST
                    (BIN 1
                         [VNUM "0"]))
                   $body)
                  $default))
  */
  {
    nl_ast __pragmas_node_start = NULL;
    nl_ast __pragmas_node_end = NULL;
    nl_ast __expr_node_start = NULL;
    nl_ast __expr_node_end = NULL;
    nl_ast __default_node_start = NULL;
    nl_ast __default_node_end = NULL;
    nl_ast __body_node_start = NULL;
    nl_ast __body_node_end = NULL;

    __tree = *__root_p;
    /* (replace */
    /* beginning pattern (CASE...) */
    do {
      nl_ast __temp = __tree;
      nl_ast __tree = __temp;
      nl_ast __prev_start = NULL;
      nl_ast __prev_end = NULL;

      /* matching "(CASE" */
      __match = 0;
      if ( __tree != NULL ) {
	nl_token __token = nl_ast_token (__tree);
	__match =  (__token == nl_token_case);
      }
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_child (__tree);

      /* beginning pattern (BITNOT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(BITNOT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_bitnot);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$expr" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __expr_node_start = __tree;
	  __expr_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (BITNOT...) */
      } while (0);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* beginning pattern (LIST...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(LIST" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_list);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* &rest */
	__prev_start = __prev_end = __tree;
	/* have to scan to the end of the list to set __prev_end */
	 if ( __prev_start != NULL ) {
	  nl_ast __temp = __prev_start;
	  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
	    __prev_end = __temp;
	  }
	}
	/* matching ")" */
	/* &rest is in effect, so always match */
	__match = 1;
	/* end pattern (LIST...) */
      } while (0);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      __pragmas_node_start = __prev_start;
      __pragmas_node_end = __prev_end;
      /* beginning pattern (CASE_ITEM...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(CASE_ITEM" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_case_item);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* beginning pattern (LIST...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(LIST" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_list);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* beginning pattern (BIN...) */
	  do {
	    nl_ast __temp = __tree;
	    nl_ast __tree = __temp;
	    nl_ast __prev_start = NULL;
	    nl_ast __prev_end = NULL;

	    /* matching "(BIN" */
	    __match = 0;
	    if ( __tree != NULL ) {
	      nl_token __token = nl_ast_token (__tree);
	      __match =  (__token == nl_token_bin);
	    }
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_child (__tree);

	    /* matching "1" */
	    __match = (nl_ast_token (__tree) == nl_token_number && nl_number_ast_value ((nl_number_ast) __tree) == 1);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching "[VNUM 1]" */
	    __match = (nl_ast_token (__tree) == nl_token_vnum && strcmp (nl_vnum_ast_bits ((nl_vnum_ast) __tree), "1") == 0);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    /* matching ")" */
	    __match = (__tree == NULL);
	    if ( ! __match ) {
	      break;
	    }
	    /* end pattern (BIN...) */
	  } while (0);
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_sibling (__tree);
	  /* matching ")" */
	  __match = (__tree == NULL);
	  if ( ! __match ) {
	    break;
	  }
	  /* end pattern (LIST...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching "$body" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __body_node_start = __tree;
	  __body_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (CASE_ITEM...) */
      } while (0);
      if ( ! __match ) {
	break;
      }
      __prev_start = __prev_end = __tree;
      __tree = nl_ast_sibling (__tree);
      /* &optional */
      /* beginning pattern (DEFAULT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(DEFAULT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_default);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* &rest */
	__prev_start = __prev_end = __tree;
	/* have to scan to the end of the list to set __prev_end */
	 if ( __prev_start != NULL ) {
	  nl_ast __temp = __prev_start;
	  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
	    __prev_end = __temp;
	  }
	}
	/* matching ")" */
	/* &rest is in effect, so always match */
	__match = 1;
	/* end pattern (DEFAULT...) */
      } while (0);
      /* &optional is in effect */
      /* only advance to sibling if matched */
      if ( __match ) {
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
      }
      __match = 1;
      __default_node_start = __prev_start;
      __default_node_end = __prev_end;
      /* matching ")" */
      __match = (__tree == NULL);
      if ( ! __match ) {
	break;
      }
      /* end pattern (CASE...) */
    } while (0);
    if ( __match ) {
      /* (replace...) rule matched, now construct replacement */
      {
	char *__file = nl_ast_file (__tree);
	int __line = nl_ast_line (__tree);
	nl_ast __new_tree_start = NULL;
	nl_ast __new_tree_end = NULL;
	nl_ast __sibling = NULL;
	__new_tree_start = nl_ast_sibling (__tree);
	__sibling = __new_tree_start;
	/* saving __sibling */
	{
	  nl_ast __sibling = NULL;
	  __new_tree_start = NULL;
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __default_node_start;
	    __new_tree_end = __default_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __body_node_start;
	      __new_tree_end = __body_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = NULL;
	      __sibling = __new_tree_start;
	      /* saving __sibling */
	      {
		nl_ast __sibling = NULL;
		__new_tree_start = NULL;
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_vnum_ast_create ("0");
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		__sibling = __new_tree_start;
		/* saving __sibling */
		{
		  nl_ast __sibling = NULL;
		  __new_tree_start = (nl_ast) nl_number_ast_create (1);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		}
		if ( __new_tree_end != NULL ) {
		  nl_ast_set_sibling (__new_tree_end, __sibling); 
		}
		/* constructing node with token BIN */
		{
		  nl_token __token = nl_token_bin;
		  nl_ast __child = __new_tree_start;
		  __new_tree_start = nl_ast_create (__token);
		  nl_ast_set_file_line (__new_tree_start, __file, __line);
		  __new_tree_end = __new_tree_start;
		  nl_ast_set_child (__new_tree_start, __child);
		}
	      }
	      if ( __new_tree_end != NULL ) {
		nl_ast_set_sibling (__new_tree_end, __sibling);	
	      }
	      /* constructing node with token LIST */
	      {
		nl_token __token = nl_token_list;
		nl_ast __child = __new_tree_start;
		__new_tree_start = nl_ast_create (__token);
		nl_ast_set_file_line (__new_tree_start, __file, __line);
		__new_tree_end = __new_tree_start;
		nl_ast_set_child (__new_tree_start, __child);
	      }
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token CASE_ITEM */
	    {
	      nl_token __token = nl_token_case_item;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __pragmas_node_start;
	    __new_tree_end = __pragmas_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __expr_node_start;
	    __new_tree_end = __expr_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  /* constructing node with token CASE */
	  {
	    nl_token __token = nl_token_case;
	    nl_ast __child = __new_tree_start;
	    __new_tree_start = nl_ast_create (__token);
	    nl_ast_set_file_line (__new_tree_start, __file, __line);
	    __new_tree_end = __new_tree_start;
	    nl_ast_set_child (__new_tree_start, __child);
	  }
	}
	if ( __new_tree_end != NULL ) {
	  nl_ast_set_sibling (__new_tree_end, __sibling); 
	}
	*__root_p = __new_tree_start;
	__tree = __new_tree_end;
      }
      /* end of (replace...) */
    }
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_flip_cases (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_flip_cases_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_flatten_concats_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            (replace
             (CONCAT $arg)
             $arg)
            (replace
             (REPEAT_CONCAT 1 $arg)
             $arg)
            (CONCAT
             (repeat
              (or
               (replace
                (CONCAT &rest =args)
                $args)
               *))))
  */
  {
    nl_ast __args_node_start = NULL;
    nl_ast __args_node_end = NULL;
    nl_ast __arg_node_start = NULL;
    nl_ast __arg_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* (replace */
      /* beginning pattern (CONCAT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(CONCAT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_concat);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "$arg" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg_node_start = __tree;
	  __arg_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (CONCAT...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __arg_node_start;
	    __new_tree_end = __arg_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* beginning pattern (REPEAT_CONCAT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(REPEAT_CONCAT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_repeat_concat);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* matching "1" */
	__match = (nl_ast_token (__tree) == nl_token_number && nl_number_ast_value ((nl_number_ast) __tree) == 1);
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching "$arg" */
	__match = (__tree != NULL);
	if ( __match ) {
	  __arg_node_start = __tree;
	  __arg_node_end = __tree;
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (REPEAT_CONCAT...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = __arg_node_start;
	    __new_tree_end = __arg_node_end;
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* beginning pattern (CONCAT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(CONCAT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_concat);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* (repeat */
	{
	  nl_ast __temp = __prev_start;
	  nl_ast __prev_start = __temp;
	  int __first = 1;
	  do {
	    /* (or */
	    do {
	      /* (replace */
	      /* beginning pattern (CONCAT...) */
	      do {
		nl_ast __temp = __tree;
		nl_ast __tree = __temp;
		nl_ast __prev_start = NULL;
		nl_ast __prev_end = NULL;

		/* matching "(CONCAT" */
		__match = 0;
		if ( __tree != NULL ) {
		  nl_token __token = nl_ast_token (__tree);
		  __match =  (__token == nl_token_concat);
		}
		if ( ! __match ) {
		  break;
		}
		__prev_start = __prev_end = __tree;
		__tree = nl_ast_child (__tree);

		/* &rest */
		__prev_start = __prev_end = __tree;
		/* have to scan to the end of the list to set __prev_end */
		 if ( __prev_start != NULL ) {
		  nl_ast __temp = __prev_start;
		  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
		    __prev_end = __temp;
		  }
		}
		__args_node_start = __prev_start;
		__args_node_end = __prev_end;
		/* matching ")" */
		/* &rest is in effect, so always match */
		__match = 1;
		/* end pattern (CONCAT...) */
	      } while (0);
	      if ( __match ) {
		/* (replace...) rule matched, now construct replacement */
		{
		  char *__file = nl_ast_file (__tree);
		  int __line = nl_ast_line (__tree);
		  nl_ast __new_tree_start = NULL;
		  nl_ast __new_tree_end = NULL;
		  nl_ast __sibling = NULL;
		  __new_tree_start = nl_ast_sibling (__tree);
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = __args_node_start;
		    __new_tree_end = __args_node_end;
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  if ( __first ) {
		    nl_ast_set_child (__prev_start, __new_tree_start);
		  }
		  else {
		    nl_ast_set_sibling (__prev_start, __new_tree_start);
		  }
		  __tree = __new_tree_end;
		}
		/* end of (replace...) */
	      }
	      if ( __match ) {
		break;
	      }
	      __match = (__tree != NULL);
	      if ( __match ) {
		break;
	      }
	      /* end of (or...) */
	    } while (0);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    __match = (__tree != NULL);
	    __first = 0;
	  /* end of (repeat...) */
	  } while (__match);

	}
	/* have to set __prev_start to point to the beginning of the repeat */
	__prev_start = nl_ast_child (__prev_start);
	/* repeat construct always matches */
	__match = 1;
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (CONCAT...) */
      } while (0);
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_flatten_concats (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_flatten_concats_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_infer_inverting_ops_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            (replace
             (BITNOT
              (AND &rest =args))
             (NAND $args))
            (replace
             (BITNOT
              (OR &rest =args))
             (NOR $args)))
  */
  {
    nl_ast __args_node_start = NULL;
    nl_ast __args_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* (replace */
      /* beginning pattern (BITNOT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(BITNOT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_bitnot);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* beginning pattern (AND...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(AND" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_and);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* &rest */
	  __prev_start = __prev_end = __tree;
	  /* have to scan to the end of the list to set __prev_end */
	   if ( __prev_start != NULL ) {
	    nl_ast __temp = __prev_start;
	    while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
	      __prev_end = __temp;
	    }
	  }
	  __args_node_start = __prev_start;
	  __args_node_end = __prev_end;
	  /* matching ")" */
	  /* &rest is in effect, so always match */
	  __match = 1;
	  /* end pattern (AND...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (BITNOT...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __args_node_start;
	      __new_tree_end = __args_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token NAND */
	    {
	      nl_token __token = nl_token_nand;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* (replace */
      /* beginning pattern (BITNOT...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "(BITNOT" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_bitnot);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* beginning pattern (OR...) */
	do {
	  nl_ast __temp = __tree;
	  nl_ast __tree = __temp;
	  nl_ast __prev_start = NULL;
	  nl_ast __prev_end = NULL;

	  /* matching "(OR" */
	  __match = 0;
	  if ( __tree != NULL ) {
	    nl_token __token = nl_ast_token (__tree);
	    __match =  (__token == nl_token_or);
	  }
	  if ( ! __match ) {
	    break;
	  }
	  __prev_start = __prev_end = __tree;
	  __tree = nl_ast_child (__tree);

	  /* &rest */
	  __prev_start = __prev_end = __tree;
	  /* have to scan to the end of the list to set __prev_end */
	   if ( __prev_start != NULL ) {
	    nl_ast __temp = __prev_start;
	    while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
	      __prev_end = __temp;
	    }
	  }
	  __args_node_start = __prev_start;
	  __args_node_end = __prev_end;
	  /* matching ")" */
	  /* &rest is in effect, so always match */
	  __match = 1;
	  /* end pattern (OR...) */
	} while (0);
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_sibling (__tree);
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern (BITNOT...) */
      } while (0);
      if ( __match ) {
	/* (replace...) rule matched, now construct replacement */
	{
	  char *__file = nl_ast_file (__tree);
	  int __line = nl_ast_line (__tree);
	  nl_ast __new_tree_start = NULL;
	  nl_ast __new_tree_end = NULL;
	  nl_ast __sibling = NULL;
	  __new_tree_start = nl_ast_sibling (__tree);
	  __sibling = __new_tree_start;
	  /* saving __sibling */
	  {
	    nl_ast __sibling = NULL;
	    __new_tree_start = NULL;
	    __sibling = __new_tree_start;
	    /* saving __sibling */
	    {
	      nl_ast __sibling = NULL;
	      __new_tree_start = __args_node_start;
	      __new_tree_end = __args_node_end;
	    }
	    if ( __new_tree_end != NULL ) {
	      nl_ast_set_sibling (__new_tree_end, __sibling); 
	    }
	    /* constructing node with token NOR */
	    {
	      nl_token __token = nl_token_nor;
	      nl_ast __child = __new_tree_start;
	      __new_tree_start = nl_ast_create (__token);
	      nl_ast_set_file_line (__new_tree_start, __file, __line);
	      __new_tree_end = __new_tree_start;
	      nl_ast_set_child (__new_tree_start, __child);
	    }
	  }
	  if ( __new_tree_end != NULL ) {
	    nl_ast_set_sibling (__new_tree_end, __sibling); 
	  }
	  *__root_p = __new_tree_start;
	  __tree = __new_tree_end;
	}
	/* end of (replace...) */
      }
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_infer_inverting_ops (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_infer_inverting_ops_postwalker, NULL);
  return root;
}




static
nl_walk_status
__ast2g_flatten_logic_trees_postwalker (nl_ast *__root_p, void *__ptr)
{
  nl_ast __tree;
  int __match;
  nl_walk_status __status = nl_walk_status_continue;

  /* rule: (or
            ((AND NAND)
             (repeat
              (or
               (replace
                (AND &rest =args)
                $args)
               *)))
            ((OR NOR)
             (repeat
              (or
               (replace
                (OR &rest =args)
                $args)
               *)))
            ((XOR XNOR)
             (repeat
              (or
               (replace
                (XOR &rest =args)
                $args)
               *))))
  */
  {
    nl_ast __args_node_start = NULL;
    nl_ast __args_node_end = NULL;

    __tree = *__root_p;
    /* (or */
    do {
      /* beginning pattern ((AND NAND)...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "((AND NAND)" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_and ||
		      __token == nl_token_nand);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* (repeat */
	{
	  nl_ast __temp = __prev_start;
	  nl_ast __prev_start = __temp;
	  int __first = 1;
	  do {
	    /* (or */
	    do {
	      /* (replace */
	      /* beginning pattern (AND...) */
	      do {
		nl_ast __temp = __tree;
		nl_ast __tree = __temp;
		nl_ast __prev_start = NULL;
		nl_ast __prev_end = NULL;

		/* matching "(AND" */
		__match = 0;
		if ( __tree != NULL ) {
		  nl_token __token = nl_ast_token (__tree);
		  __match =  (__token == nl_token_and);
		}
		if ( ! __match ) {
		  break;
		}
		__prev_start = __prev_end = __tree;
		__tree = nl_ast_child (__tree);

		/* &rest */
		__prev_start = __prev_end = __tree;
		/* have to scan to the end of the list to set __prev_end */
		 if ( __prev_start != NULL ) {
		  nl_ast __temp = __prev_start;
		  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
		    __prev_end = __temp;
		  }
		}
		__args_node_start = __prev_start;
		__args_node_end = __prev_end;
		/* matching ")" */
		/* &rest is in effect, so always match */
		__match = 1;
		/* end pattern (AND...) */
	      } while (0);
	      if ( __match ) {
		/* (replace...) rule matched, now construct replacement */
		{
		  char *__file = nl_ast_file (__tree);
		  int __line = nl_ast_line (__tree);
		  nl_ast __new_tree_start = NULL;
		  nl_ast __new_tree_end = NULL;
		  nl_ast __sibling = NULL;
		  __new_tree_start = nl_ast_sibling (__tree);
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = __args_node_start;
		    __new_tree_end = __args_node_end;
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  if ( __first ) {
		    nl_ast_set_child (__prev_start, __new_tree_start);
		  }
		  else {
		    nl_ast_set_sibling (__prev_start, __new_tree_start);
		  }
		  __tree = __new_tree_end;
		}
		/* end of (replace...) */
	      }
	      if ( __match ) {
		break;
	      }
	      __match = (__tree != NULL);
	      if ( __match ) {
		break;
	      }
	      /* end of (or...) */
	    } while (0);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    __match = (__tree != NULL);
	    __first = 0;
	  /* end of (repeat...) */
	  } while (__match);

	}
	/* have to set __prev_start to point to the beginning of the repeat */
	__prev_start = nl_ast_child (__prev_start);
	/* repeat construct always matches */
	__match = 1;
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern ((AND NAND)...) */
      } while (0);
      if ( __match ) {
	break;
      }
      /* beginning pattern ((OR NOR)...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "((OR NOR)" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_or ||
		      __token == nl_token_nor);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* (repeat */
	{
	  nl_ast __temp = __prev_start;
	  nl_ast __prev_start = __temp;
	  int __first = 1;
	  do {
	    /* (or */
	    do {
	      /* (replace */
	      /* beginning pattern (OR...) */
	      do {
		nl_ast __temp = __tree;
		nl_ast __tree = __temp;
		nl_ast __prev_start = NULL;
		nl_ast __prev_end = NULL;

		/* matching "(OR" */
		__match = 0;
		if ( __tree != NULL ) {
		  nl_token __token = nl_ast_token (__tree);
		  __match =  (__token == nl_token_or);
		}
		if ( ! __match ) {
		  break;
		}
		__prev_start = __prev_end = __tree;
		__tree = nl_ast_child (__tree);

		/* &rest */
		__prev_start = __prev_end = __tree;
		/* have to scan to the end of the list to set __prev_end */
		 if ( __prev_start != NULL ) {
		  nl_ast __temp = __prev_start;
		  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
		    __prev_end = __temp;
		  }
		}
		__args_node_start = __prev_start;
		__args_node_end = __prev_end;
		/* matching ")" */
		/* &rest is in effect, so always match */
		__match = 1;
		/* end pattern (OR...) */
	      } while (0);
	      if ( __match ) {
		/* (replace...) rule matched, now construct replacement */
		{
		  char *__file = nl_ast_file (__tree);
		  int __line = nl_ast_line (__tree);
		  nl_ast __new_tree_start = NULL;
		  nl_ast __new_tree_end = NULL;
		  nl_ast __sibling = NULL;
		  __new_tree_start = nl_ast_sibling (__tree);
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = __args_node_start;
		    __new_tree_end = __args_node_end;
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  if ( __first ) {
		    nl_ast_set_child (__prev_start, __new_tree_start);
		  }
		  else {
		    nl_ast_set_sibling (__prev_start, __new_tree_start);
		  }
		  __tree = __new_tree_end;
		}
		/* end of (replace...) */
	      }
	      if ( __match ) {
		break;
	      }
	      __match = (__tree != NULL);
	      if ( __match ) {
		break;
	      }
	      /* end of (or...) */
	    } while (0);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    __match = (__tree != NULL);
	    __first = 0;
	  /* end of (repeat...) */
	  } while (__match);

	}
	/* have to set __prev_start to point to the beginning of the repeat */
	__prev_start = nl_ast_child (__prev_start);
	/* repeat construct always matches */
	__match = 1;
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern ((OR NOR)...) */
      } while (0);
      if ( __match ) {
	break;
      }
      /* beginning pattern ((XOR XNOR)...) */
      do {
	nl_ast __temp = __tree;
	nl_ast __tree = __temp;
	nl_ast __prev_start = NULL;
	nl_ast __prev_end = NULL;

	/* matching "((XOR XNOR)" */
	__match = 0;
	if ( __tree != NULL ) {
	  nl_token __token = nl_ast_token (__tree);
	  __match =  (__token == nl_token_xor ||
		      __token == nl_token_xnor);
	}
	if ( ! __match ) {
	  break;
	}
	__prev_start = __prev_end = __tree;
	__tree = nl_ast_child (__tree);

	/* (repeat */
	{
	  nl_ast __temp = __prev_start;
	  nl_ast __prev_start = __temp;
	  int __first = 1;
	  do {
	    /* (or */
	    do {
	      /* (replace */
	      /* beginning pattern (XOR...) */
	      do {
		nl_ast __temp = __tree;
		nl_ast __tree = __temp;
		nl_ast __prev_start = NULL;
		nl_ast __prev_end = NULL;

		/* matching "(XOR" */
		__match = 0;
		if ( __tree != NULL ) {
		  nl_token __token = nl_ast_token (__tree);
		  __match =  (__token == nl_token_xor);
		}
		if ( ! __match ) {
		  break;
		}
		__prev_start = __prev_end = __tree;
		__tree = nl_ast_child (__tree);

		/* &rest */
		__prev_start = __prev_end = __tree;
		/* have to scan to the end of the list to set __prev_end */
		 if ( __prev_start != NULL ) {
		  nl_ast __temp = __prev_start;
		  while ( (__temp = nl_ast_sibling (__temp)) != NULL ) {
		    __prev_end = __temp;
		  }
		}
		__args_node_start = __prev_start;
		__args_node_end = __prev_end;
		/* matching ")" */
		/* &rest is in effect, so always match */
		__match = 1;
		/* end pattern (XOR...) */
	      } while (0);
	      if ( __match ) {
		/* (replace...) rule matched, now construct replacement */
		{
		  char *__file = nl_ast_file (__tree);
		  int __line = nl_ast_line (__tree);
		  nl_ast __new_tree_start = NULL;
		  nl_ast __new_tree_end = NULL;
		  nl_ast __sibling = NULL;
		  __new_tree_start = nl_ast_sibling (__tree);
		  __sibling = __new_tree_start;
		  /* saving __sibling */
		  {
		    nl_ast __sibling = NULL;
		    __new_tree_start = __args_node_start;
		    __new_tree_end = __args_node_end;
		  }
		  if ( __new_tree_end != NULL ) {
		    nl_ast_set_sibling (__new_tree_end, __sibling); 
		  }
		  if ( __first ) {
		    nl_ast_set_child (__prev_start, __new_tree_start);
		  }
		  else {
		    nl_ast_set_sibling (__prev_start, __new_tree_start);
		  }
		  __tree = __new_tree_end;
		}
		/* end of (replace...) */
	      }
	      if ( __match ) {
		break;
	      }
	      __match = (__tree != NULL);
	      if ( __match ) {
		break;
	      }
	      /* end of (or...) */
	    } while (0);
	    if ( ! __match ) {
	      break;
	    }
	    __prev_start = __prev_end = __tree;
	    __tree = nl_ast_sibling (__tree);
	    __match = (__tree != NULL);
	    __first = 0;
	  /* end of (repeat...) */
	  } while (__match);

	}
	/* have to set __prev_start to point to the beginning of the repeat */
	__prev_start = nl_ast_child (__prev_start);
	/* repeat construct always matches */
	__match = 1;
	/* matching ")" */
	__match = (__tree == NULL);
	if ( ! __match ) {
	  break;
	}
	/* end pattern ((XOR XNOR)...) */
      } while (0);
      if ( __match ) {
	break;
      }
      /* end of (or...) */
    } while (0);
  }
  return nl_walk_status_continue;
}

nl_ast
ast2g_flatten_logic_trees (nl_ast tree)
{
  nl_ast root = tree;
  nl_ast_walk (&root, NULL, __ast2g_flatten_logic_trees_postwalker, NULL);
  return root;
}



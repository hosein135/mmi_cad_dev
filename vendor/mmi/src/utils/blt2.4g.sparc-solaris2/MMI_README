In order to run the configure, use the line:

./configure --with-tcl=/mmi-cad/utils/tcltk/install.sparc-solaris2 --with-tk=/mmi-cad/utils/tcltk/install.sparc-solaris2 --with-cc=gcc

Note that the install directory must have correct
tclConfig.sh/tkConfig.sh files